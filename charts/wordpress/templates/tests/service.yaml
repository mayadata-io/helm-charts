apiVersion: v1
kind: Service
metadata:
  name: {{ include "wordpress-nfs-galera.fullname" . }}
  labels:
    {{- include "wordpress-nfs-galera.labels" . | nindent 4 }}
spec:
  type: {{ .Values.service.type }}
  ports:
    - port: {{ .Values.service.port }}
      targetPort: http
      protocol: TCP
      name: http
  selector:
    {{- include "wordpress-nfs-galera.selectorLabels" . | nindent 4 }}