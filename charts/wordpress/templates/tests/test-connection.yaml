apiVersion: v1
kind: Pod
metadata:
  name: "{{ include "wordpress-nfs-galera.fullname" . }}-test-connection"
  labels:
    {{- include "wordpress-nfs-galera.labels" . | nindent 4 }}
  annotations:
    "helm.sh/hook": test
spec:
  containers:
    - name: wget
      image: busybox
      command: ['wget']
      args: ['{{ include "wordpress-nfs-galera.fullname" . }}:{{ .Values.service.port }}']
  restartPolicy: Never
